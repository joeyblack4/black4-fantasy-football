# Black4 Fantasy Football

## Governing principle — fast and loose

Read docs/OWNER_CHARTER.md for Joey's current owner instructions and docs/OPERATING_PRINCIPLE.md for development practice. Enable each native model/harness to operate freely with its own tools, planning, memory and delegation. Spend caps are the discretionary runtime constraint. Avoid custom approval layers, rigid helper allowlists, generic cognition wrappers and hypothetical safety infrastructure. Build thin connections; test material failures. Preserve account/workspace separation, league correctness, no duplicate actions, honest cost evidence and current authenticated league restrictions. Reference-priced subscription usage is reporting, not an additional automatic cap.

## Current priority — equal regular-season infrastructure

September 9 instruction: the production draft is complete. Implement the approved common season surface: authoritative readable league state, clear current instructions, owner-equivalent actions, and durable owner-controlled appointments delivered into the existing Buzz-managed native runtimes. Read docs/SEASON_OPERATIONS.md for current owner access. Prior migration-only scope and pre-launch draft holds are historical; do not apply them to ordinary authorized season management.

Preserve each assigned model, native harness, workspace, tools, memory, research choices and existing spending limits. Black4 supplies equal infrastructure; owners choose their strategy and whether and when to use scheduling. Do not create default recurring season reminders, pick lineups, supply common scouting recommendations or repair football mistakes for an owner.

The current field is eleven AI companies and Joey. Buzz owns native start, stop and restart. Do not revive retired generic owner loops or bypass deliberate runtime stops. Implement PostgreSQL-backed scheduling and observable delivery using the current native connection; synthetic tests and accepted jobs are not evidence that a model actually ran.

Build a public experiment with 11 AI franchises and 1 human franchise. Preserve each assigned lead model; native same-company helpers are allowed. Record model provenance without adding per-helper approval. Spend caps and actual league ownership are enforced by services, not prompts. Keep synthetic demonstrations clearly labeled. Never invent agent messages, measured outcomes, live stats, or authenticated model access.

Current decisions are in docs/DECISIONS.md; PROJECT_BRIEF.md points to the current plan; the early brief is archived. The league is operating; verify each infrastructure capability independently before claiming it is live. The approved season infrastructure plan authorizes league-only implementation and the same neutral capability announcement to all owners. Actual public batches require Joey’s exact approval; account funding and commercial subscriptions follow the accepted budget and concrete account handoffs. Customer infrastructure is outside this scope. Record account/payment dependencies in docs/JOEY_ACTIONS.md.

Use TypeScript, PostgreSQL, versioned SQL migrations, and shared command handlers for HTTP, CLI and MCP. Use atomic transactions, idempotency keys with payload conflict detection, verified actors, server-controlled clocks, and receipts. Never accept client supplied actor authority through HTTP bodies.

Tests should cover concurrency, replay, invalid transitions, ownership boundaries and recovery. Use isolated test schemas, never delete another project's data. Preserve unrelated work. Keep adapters replaceable so the public repository runs with synthetic data and needs no customer code.

Parallel ownership for this build: league agent owns src/league, migrations/001_league.sql, tests/league*; runtime agent owns src/runtime, migrations/002_runtime.sql, tests/runtime*; integration agent owns src/data, src/buzz, migrations/003_data.sql, tests/data*, tests/buzz*, docs/INTEGRATIONS.md. Root owns shared database, transports, UI, docs, and integration validation. Coordinate interface changes explicitly.
