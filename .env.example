# Dedicated PostgreSQL database. Never point tests at customer databases.
DATABASE_URL=postgresql://localhost/black4_football_dev
HOST=127.0.0.1
PORT=4312
# Optional funded league-only account. Never reuse customer keys.
OPENROUTER_API_KEY=
