# MFL real-model draft rehearsal

Prepared September 8, 2026, 07:05 UTC. No league changes, picks, observer configuration, model calls or resets were performed by this readiness audit. Private receipts: `.local/live/mfl/rehearsal-readcheck.json` and `rehearsal-db-read.json`. The rehearsal remains disabled.

## Current verified state

| Surface                                                                | Read-only result                                                                                                                                                                                                                                                                                       |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Real league [62282](https://www43.myfantasyleague.com/2026/home/62282) | Exact 2026/www43 session binding; league, abilities, calendar and static draft reads returned 200. Twelve teams, roster16, QB/RB/RB/WR/WR/TE/PK/Def starters. Native mode is **email**, not live. Static XML has 192 scheduled empty slots, round1/pick1/franchise0007; these are not drafted players. |
| Disposable [46625](https://www43.myfantasyleague.com/2026/home/46625)  | Exact 2026/www43 session binding; same four reads returned 200. Native mode live, timer OFF, player pool Both, previous 192-pick scripted draft complete. It needs a deliberate reviewed reset before another draft.                                                                                   |
| Authentication                                                         | Both `abilities` reads identify effective franchise0001. Calendar is an owner-restricted endpoint and returned data. This verifies present authenticated reading, not lifetime renewal or every delegated write. The earlier actual write receipts establish tested delegation.                        |
| Common player catalog                                                  | Official public 2026 export returned 2,621 entries. Includes QB/RB/WR/TE/PK/Def plus IDP, Coach and team-unit positions; the unfiltered pool is not the ratified eligible list. Player metadata is catalog data, not a current availability or injury feed.                                            |
| Local production                                                       | Host binding v1 selects62282; writesEnabled=false. No draft observer configured. Current owner onboarding stage blocks football. Ten AI runtime identities and two human identities exist; the second human retains its unassigned routing ID.                                                         |

## Required native settings and operator duties

Apply the owners' reviewed, host-compatible rules to disposable46625 first. Preserve a backup and the prior 192-pick fixture receipt before any destructive reset. Explicitly verify native live mode, exact draft order, roster size, position eligibility, empty rosters, empty completed-pick history and first on-clock franchise. Keep real62282 untouched during rehearsal.

The candidate constitution offers a disabled native timer and an operator target of120 or180 seconds. Those targets are **not** automatic deadlines. Existing trial evidence confirms timerOFF only. The previously inspected native help says timeout selection can consult the Work List then My Draft List and an empty-list fallback; the proposed configuration is Skip with all automatic-on-clock flags off, no Fantasy Sharks/ADP selection. A later authenticated native inspection at07:42UTC verified timerOFF, AUTO_PICK=NO/Skip, unlimited consecutive timeouts and all twelve automatic-on-clock flags unchecked. Exact forms and receipts are in [the native reset procedure](MFL_TRIAL_RESET_PROCEDURE.md). Timed expiration and personal owner-list/away behavior remain untested; recheck the settings after any reset or save. Local `mflLocalDraftQueue` is a private preference list, not a native queue or automatic pick authorization.

MFL's [official live-draft integration guide](https://api.myfantasyleague.com/2026/support?FAQ=935), refreshed read-only during this audit, documents static results/status files, roughly five-second observation, and commissioner-only pause/resume/undo/skip. Pause is documented for a running timer; do not rely on it to stop timerOFF owner workers. **Stop owner dispatch and the observer first** when holding this rehearsal. No blank queue should silently invoke an external ranking or invented owner decision.

## Chosen execution plan: one canonical database and wallet ledger

The rehearsal uses the **same canonical database, runtime owner identities and wallets**. Disposable MFL46625 is a temporary, explicitly authorized football destination. There is no cloned database, second wallet balance or cross-database settlement. All real provider calls, research credits, reservations and uncertain holds remain in their existing canonical ledgers.

1. Finish the rule-alignment checkpoint and owner qualification. Pause production owner workers and football dispatch; stop any observer. Preserve pending production work without replaying it as rehearsal. Take a private verified backup and capture the current62282 host binding, deployment reference, worker settings and owner-stage state.
2. Configure a narrowly scoped rehearsal gate with a unique rehearsal ID/epoch, exact disposable46625/year2026/www43 identity, expected host version, participant mapping and approved per-owner turn/spend caps. Only jobs belonging to that rehearsal may claim or commit rehearsal football actions; stale onboarding jobs, unrelated appointments, governance, marketing and public publication stay blocked. The same-DB service and research/observer/receipt hooks are implemented and tested locally. `RehearsalRuntime.arm(commissioner, {epoch, expectedHostVersion, trialConfigRef, capMicros, operatorEvidenceRef, reason})` initially capped at5,000,000micros per owner. Root selected a common20,000,000micros ($20) cap for the full real-model draft; that increase and rehearsal-only successful-receipt wake suppression are pending their runtime patch/tests. Verify the final deployed schema/contract before use. No rehearsal has been activated.
3. Once no owner or football dispatch is in flight, use the reviewed host-binding operation to temporarily select46625 under a distinct configRef. Point only the explicitly stopped/reconfigured processes to a separate trial deployment file and `.local/mfl/session.json`. Its session binding must match46625/2026/www43. Preserve the original62282 deployment/session files. Verify the twelve native franchise mappings and the agreed order. Binding changes hold prior football intents; do not reinterpret them against the new host.
4. Run normal model and research reservations against the **existing canonical wallets**. Prior actual spending and unknown holds remain charged/reserved; they are neither reset nor reclassified. Enforce the additional rehearsal cap against these same liabilities. Keep existing restricted provider keys, exact models and endpoint pins. A rehearsal spending limit does not create new account credit or an independent season allowance.
5. Give all ten real models the same draft tools and research access. Label the football environment as a rehearsal while preserving real model provenance. Do not mark real model outputs synthetic. Explicitly label operator-authored human-seat picks as rehearsal stand-ins, never Chris's decisions or votes. Use native MFL for these manual picks; no custom human draft client is required tonight.
6. Rehearsal chat must not release unrelated production jobs. Use an explicitly bound private rehearsal room if collaboration is included, or omit chat from this mechanical test. Do not run a second production listener or broadcast wakeups. Preserve genuine decisions and receipts in the same canonical archive with rehearsal/host-version identifiers.
7. After the operator deliberately resets/configures46625 and independently reads back empty rosters/history, prepare a new observer epoch and the actual temporary host version. Keep writes disabled for the initial read-only readiness check. Only explicitly enable rehearsal workers, trial writes and observation after native mode/order, wallet limits, rule choices and human handling pass. Verify the first model -> football outbox -> native pick -> readback -> next-owner wake before proceeding through the full draft.
8. At completion require the agreed number of picks, unique player IDs, exact native roster sizes, every AI pick's real model receipt, every stand-in's operator receipt, no unresolved write outcome and reconciled or fully reserved costs. Stop rehearsal jobs/dispatch and the observer. Restore62282 through a new reviewed host-binding version, restore its original read-only deployment, and independently verify it remains unchanged. Stale rehearsal intents stay held; nothing is replayed into62282. Production resumes only after its separate configuration and release checkpoint.

## Human entry and control defaults

Use native MFL from the correct league home above, then its Live Draft room while authenticated. For the disposable rehearsal, Joey/operator can use the supported commissioner controls to enter clearly labeled stand-in picks for the two human seats. No separate Chris identity or proxy vote is implied. Actual live-season Chris access still requires his own onboarding. The Black4 `/play` page currently submits custom-engine draft commands; it is **not** an MFL human draft client. Do not direct humans there for native MFL picks tonight.

## Remaining software/configuration gaps

- Verify the deployed same-database rehearsal gate matches the tested source, including the pending common$20 cap and successful-receipt wake suppression. Existing tested hooks cover job selection, action scope, model/research liability, atomic observer wakes, manual human turns and restoration fencing.
- A reviewed setup/hold/restore operation that cannot revive production onboarding or unrelated Buzz jobs. No wallet-copy or cross-database funding bridge is required.
- A deliberate trial reset and post-reset readback. Global native auto-pick/fallback settings were verified at07:42UTC; personal live-room away/list state remains unverified.
- Rules-derived eligibility filtering. `mfl_read players` currently filters by requested position/search, not ratified eligibility or free-agent availability. Owners must combine eligible positions with current rosters/draft history; native rejection is not a substitute for an accurate list.
- Session expiration/renewal and automated stall alerts remain unverified. With timerOFF, a failed owner cannot advance the draft automatically.

The native-live-mode software guard is implemented: configure and polling reject email mode even when static XML exposes a future turn; a mode change holds the observer, and draft-write preflight refuses before POST. Other MFL reads remain usable. Synthetic tests cover these boundaries; they are not a real-model rehearsal result.

## Full-draft economics checkpoint

Keep a fresh real-model decision for every AI pick. Existing saved queues are preferences only (`automaticPickAuthorized:false`), so they do not authorize a deterministic autopick executor. A16-round rehearsal comprises160 AI decisions and32 clearly operator-labelled human fixture picks; an18-round roster would require180AI decisions and36human fixtures. None of those fixture actions is Chris’s real strategic choice.

The initial$5 epoch cap with a$3 turn reservation cannot support16 turns at approximately$1 each: once prior liability exceeds$2, another reservation cannot start. The chosen common$20 per-owner epoch cap uses the existing$600 season wallet and existing$40 provider key; no new funding or wallet reset. Fresh authenticated key headroom is independently required. Successful pick/queue receipts should remain available on the next observer turn without causing another model call solely for acknowledgement; this suppression is pending the scoped runtime patch and must not hide failures or uncertain writes.

After the first two actual draft turns for every owner, compare measured committed model/research cost and unknown holds against remaining picks and full next-turn reservation. Record sample size and per-model observations. Stop or pause at the common cap/headroom boundary instead of promising completion from onboarding averages, silently raising limits or asking for additional funding. The full rehearsal remains unrun.
