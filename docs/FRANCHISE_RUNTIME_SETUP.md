## Subscription-first amendment — September 8, 2026

> Historical setup/draft record. Preserved evidence below describes its dated phase and does not impose current holds. The production draft is complete; use [SEASON_OPERATIONS.md](SEASON_OPERATIONS.md) for regular-season authority and tools.
> Joey requests existing Codex/Claude subscription access where the exact native integration supports it. Track actual cash separately from reference-priced competition resources; no automatic API fallback or paid overages. See [subscription and budget plan](SUBSCRIPTION_BUDGET_PLAN.md). Authentication and accounting remain unimplemented/unqualified.

## Final lineup decision — September 8, 2026

The selected profile is `buzz-compatible-v2`: seven company-native harnesses and Goose for Meta, Z.ai and DeepSeek. This supersedes the eight-native/two-Goose selection below. Model/company identities are preserved. DeepSeek’s preview harness and the custom Muse/ZCode bridges are deferred. See [final selection and rejected alternatives](FINAL_HARNESS_SELECTION.md). No production activation is implied.

## Current lineup override — September 8, 2026

Meta and Z.ai now use Goose under Joey’s explicit simplification instruction; older Muse Code/ZCode-specific setup below is historical. Meta needs an exact-model Meta API qualification; Z.ai first uses Goose’s OpenRouter route with existing league credentials assessed for reuse. No ZCode subscription or native app installation is needed for this selection. See [Buzz-first lineup](BUZZ_FIRST_LINEUP.md).

# Native runtime setup handoff

September 8, 2026. No signups or purchases have been made. The new `B4_LEAGUE_NATIVE_*` names are secret references only; no credential values exist in the prepared files. An installed CLI or personal subscription is not evidence that this franchise can access its exact assigned model under a separately measurable wallet.

## Account and integration blockers

| Franchise | Setup needed if existing access cannot be qualified                                                                                                              | Exact unresolved issue                                                                                                                                                         |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| OpenAI    | Dedicated API project/credential usable by Codex; documented noninteractive `CODEX_API_KEY`, or a supported account route with accepted cost attribution.        | Astra entitlement, native request ID and current Flex tier equivalence; separate franchise billing. Codex 0.148.0 is installed.                                                |
| Anthropic | Dedicated Anthropic API workspace/key or separately attributable supported Claude Code account.                                                                  | Exact Fable 5.1 revision, model/helper pinning and metering. Claude Code 2.1.258 is installed. Ambient key presence is not authorization to reuse it.                          |
| Google    | Gemini API key with exact model access, or Vertex project/region/credential selected deliberately.                                                               | Direct Gemini 3.8 Flash identity and equivalent route; install/pin Gemini CLI.                                                                                                 |
| xAI       | Scoped xAI API account/key or documented authenticated Grok Build account.                                                                                       | Grok Build defaults to a different lead model; establish exact Grok 4.6 support and search/helper policy. Install/pin CLI and qualify ZDR.                                     |
| Meta      | Meta Model API account with Muse Spark access; Muse supports supplying the key securely via stdin.                                                               | Exact Muse Spark 1.3 request ID, billing and session/approval model identity. Muse Code 1.0.2 is installed; no Meta authentication was attempted.                              |
| DeepSeek  | Dedicated DeepSeek platform credential if moving from current DeepInfra route.                                                                                   | Native dated Pro 0813 identity versus moving aliases; preview harness installation/version and full-feature profile. Current live route is DeepInfra FP8, not direct DeepSeek. |
| Qwen      | Alibaba ModelStudio account/key with appropriate regional endpoint and model catalog, or requalify the existing OpenRouter route inside Qwen Code.               | Qwen 3.8 Max 0902 access; Standard API/Coding Plan/Token Plan are not interchangeable. Old OAuth flow is discontinued. Install/pin Qwen Code.                                  |
| Mistral   | Scoped Mistral account/API key or qualifying supported Vibe subscription.                                                                                        | Medium 3.5 exact mapping and ZDR; install/pin Vibe; enforce spending outside its potentially incomplete price estimates.                                                       |
| Kimi      | Kimi Code account/key configured through the documented provider settings, or verified custom route.                                                             | Kimi K3 model alias/key/endpoint, coding-plan entitlement and billing. Do not assume shell `KIMI_API_KEY` is automatically consumed. Install/pin CLI.                          |
| Z.ai      | ZCode native account/setup, then prove supported unattended integration; if absent, qualify officially supported OpenCode with correct Z.AI/Coding Plan account. | Supported headless protocol and exact GLM 5.3 pin; multimodal defaults may invoke Flash. No substitute is selected automatically.                                              |

Vendor setup sources, current model caveats and the underlying research are in [FRANCHISE_HARNESS_RESEARCH.md](FRANCHISE_HARNESS_RESEARCH.md). This is a qualification list, not a request to buy ten new subscriptions immediately. Native harness choice and inference routing are separate. Some existing routes may remain usable, but prior OpenRouter evidence cannot certify a different native runtime.

## One policy question

The repo currently requires every reasoning call, including helpers, to use the owner's exact assigned model. Native harnesses can use a different model from the same company for compaction, multimodal work, search or subagents. Joey has been asked whether to preserve the exact-model rule or permit same-company models within the existing wallet. Until answered, the exact-model rule stays in force. Model aliases or defaults that change identity are not silently accepted.

## Engineering blockers remain with the implementation

Credentials alone will not unblock production. The trusted isolated launcher, native session adapters, identity and hard budget broker, provider reconciliation, and real-model recovery canaries still need implementation/qualification. These are engineering responsibilities, not reasons to ask Joey for repeated generic permission. Existing uncertain charges and MFL intent receipts remain intact. Production draft actions are held regardless of account setup.
