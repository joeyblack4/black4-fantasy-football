# Implementation log — September 7 overnight build

## Implemented paths under verification

- Provider identity manifests, key fingerprints, exact endpoint/quantization routing, model/provider response checks, per-generation receipts, model/harness/bridge disclosures. Staged canaries use their own runtime jobs and the same franchise wallet; ordinary work cannot be consumed by a canary.
- Bounded OpenRouter read-tool loop with cumulative cost accounting. Official NFL source retrieval records URLs/freshness and excludes arbitrary network access. General web search is explicitly unavailable until a legitimate provider is configured.
- Urgent/normal/background jobs, same-model staff reports, governance outbox, inert brand artifacts, service requests and immutable public-content approval batches.
- Buzz archive, membership-scoped messages, managed ACP identity handoff, polling listener and outbound-message delivery work. Desktop-managed identity avoids manually copying agent keys. Live two-owner collaboration is not yet proven.
- Draft pause/resume, queue-exhaustion pause, scoped durable event delivery, dropped-player holds, trade deadlines and a ratified deterministic regular-season schedule. Postseason bracket execution remains future work; rules must be agreed before draft.
- Private `/play` controls and `/review` convention/content/service controls. API and MCP share authoritative services. Public projection is a separate read surface; unpublished content and licensed raw payloads are excluded.
- X publishing accepts only exact approved scheduled text, confirms the account, and retains uncertain delivery rather than blindly resending. OAuth credentials and actual approved post remain pending.
- Container/launchd supervision templates and encrypted age backup/restore scripts. Isolated restore, tamper rejection and loopback checks have evidence. Off-device destination and remote restore drill remain pending.
- Website in isolated branch `codex/football-launch` at `../black4-site-football`. Static build passed and the current rail highlights the matching model logo. Joey subsequently confirmed that the page still needs the actual Black4 brand treatment and logos sliding in from the right and dropping into place; these requirements remain open in `BACKLOG.md`. Public model/harness disclosures read only the sanitized projection.

- Shared-wallet subscription expense reservations, invoice reconciliation and unknown-charge holds are enforced alongside inference. No external purchase was made.
- The dedicated local live-purpose PostgreSQL/API stack is running. Bootstrap created ten unactivated AI seats, Joey Sterling and Chris Schaaf (Chris’s Buzz/login access is pending), with no players, votes or provider calls. All ten approved Buzz identities were created and their managed bridges passed startup initialization. The convention remains paused at Joey’s requested checkpoint.

## Not yet established

No authenticated model canaries, active model manifests, actual owner-authored constitution/brands/queues, real Buzz peer exchange, authenticated NFL fixture, live scoring reconciliation, approved public X receipt, verified lead inbox receipt, full real-provider draft rehearsal, unattended live soak, or off-device restore has been claimed.

Do not promote any of those gates based on synthetic tests. Account setup and review checkpoints are in `JOEY_ACTIONS.md`.

## OpenRouter account checkpoint

Ten separately restricted $40 inference keys and one operator management credential are configured. Authenticated read-back verified exact canonical model/provider guardrail assignments and key limits for all ten franchises; the account UI independently showed $0 usage. Ten model/harness manifests are staged with read-only test permissions. Live database read-back showed zero provider calls, zero jobs and zero governance meetings. See `OPENROUTER_CHECKPOINT.md` for receipts and the proposed next test session. No paid inference or negative probe has run.

Guardrail handling now preserves bounded private rejection evidence, links adjudication to the exact request, key, manifest, inspection and billing reservation, and rejects bare passing flags during activation. PostgreSQL owns evidence freshness checks, including host-clock skew regression coverage. Connectivity canaries use a separate prompt excluding founding work and reject all proposed actions.

Checkpoint validation: TypeScript and 218 tests across 29 files passed against the isolated PostgreSQL verification database. All ten Buzz bridge configurations remain `bootstrapOnly: true`.
